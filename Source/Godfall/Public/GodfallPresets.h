#pragma once

#include <EngineMinimal.h>

namespace GodfallPresets
{
	const FName OverlapAll = TEXT("OverlapAll");
	const FName OverlapAllDynamic = TEXT("OverlapAllDynamic");
	const FName BlockAll = TEXT("BlockAll");
	const FName BlockAllDynamic = TEXT("BlockAllDynamic");
	const FName CharacterMesh = TEXT("CharacterMesh");

	const FName HitTrigger = TEXT("HitTrigger");
	const FName Disable = TEXT("Disable");
	const FName SweepPawn = TEXT("SweepPawn");
	//const FName SweepStatic = TEXT("SweepStatic");
	const FName SweepCollision = TEXT("SweepCollision");
}
